<script setup>
import AppIcon from '../../../components/AppIcon.uvue'

defineProps({
  iconName: {
    type: String,
    default: ''
  },
  iconSrc: {
    type: String,
    default: ''
  },
  label: {
    type: String,
    default: ''
  },
  active: {
    type: Boolean,
    default: false
  }
})

defineEmits(['click'])
</script>

<template>
  <view :class="['tool-button', { active }]" @click="$emit('click')">
    <view class="tool-icon">
      <AppIcon v-if="iconName || iconSrc" :name="iconName" :src="iconSrc" size="40rpx" />
    </view>
    <text class="tool-label">{{ label }}</text>
  </view>
</template>

<style lang="scss" scoped>
.tool-button {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 120rpx;
  height: 120rpx;
  border-radius: $radius-lg;
  background: $bg-primary;
  box-shadow: $shadow-sm;

  &.active {
    background: rgba($brand-primary, 0.1);
  }
}
.tool-icon {
  margin-bottom: 4rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
.tool-label {
  font-size: $font-size-xs;
  color: $text-secondary;
  text-align: center;
}
</style>
