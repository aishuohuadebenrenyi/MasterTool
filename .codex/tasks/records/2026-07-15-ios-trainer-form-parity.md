# 2026-07-15 iOS 培训师表单与新建活动流程全量对齐

## 目标

- 活动库“新建活动”直接进入普通编辑页面，不经过无意义的单选项半弹窗。
- 以小程序为基线，对齐 iOS 全部培训师表单字段、选项、默认值与校验。
- 活动新建、更新和删除复用现有 CloudBase action，不伪造成功。

## 范围

- 修改 `IOS-APP/` 客户端、测试和相关文档。
- 不修改参与者页面、小程序业务流程、CloudBase action 或 `sessionId` 主链路。
- 仅活动保存与删除新增 iOS Repository 方法；其他尚未接入能力保持显式禁用。

## 实际修改

- `PrepareView` 的新建活动入口直接 push `ActivityEditor`，模板 sheet 仅保留方案选择。
- 活动模型和表单补齐场景、难度、人数、时长、目标、规则、复盘问题和 Tips；通过现有 action 真实保存、回读与删除。
- 方案、资料、帮助、复盘、反馈、设置和现场工具输入项按小程序字段收敛。
- 新增复用的选择标签组和带字数统计多行输入，现场工具共用页面级状态。
- 更新字段基线和导航审计文档。

## 验证结果

- `swift test`：8 项通过。
- iOS Simulator Debug `xcodebuild`：通过。
- `npm run verify:all`：lint、37 项单元测试和发布契约通过。
- 字段及 sheet 静态检查：新建活动仅保留普通路由，活动表单和现场工具关键字段完整。
- `git diff --check`：通过。

## 后续验证

- 标准/Max iPhone、iPad、横屏、动态字体、键盘和分屏仍需人工真机或模拟器逐项覆盖。
- 真实活动保存需要测试环境 HTTPS 入口、令牌和已部署函数后完成 E2E。
