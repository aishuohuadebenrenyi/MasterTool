<sub-page-layout
  page-class="settings-page"
  title="设置"
  scroll-class="settings-scroll"
  content-class="settings-body"
  bindback="goBack"
>
  <view wx:for="{{sections}}" wx:key="title" class="section-card">
    <view class="section-title">{{item.title}}</view>
    <view wx:for="{{item.items}}" wx:for-item="setting" wx:key="key" class="setting-row" data-key="{{setting.key}}" bindtap="toggleSetting">
      <view class="setting-info">
        <view class="setting-title">{{setting.title}}</view>
        <view class="setting-desc">{{setting.desc}}</view>
      </view>
      <view class="switch {{setting.enabled ? 'active' : ''}}"><view></view></view>
    </view>
  </view>
  <view class="section-card">
    <view class="section-title">iOS 账号互通</view>
    <view class="binding-desc">在 iOS App 的“我的 - 设置”输入一次性绑定码，即可查看同一方案、场次与复盘数据。</view>
    <view class="binding-action" bindtap="createIOSBindingCode">生成并复制绑定码</view>
    <view wx:if="{{iosBindingCode}}" class="binding-code">{{iosBindingCode}}</view>
    <view wx:if="{{iosBindingExpiresAt}}" class="binding-expire">{{iosBindingExpiresAt}}</view>
  </view>
</sub-page-layout>
