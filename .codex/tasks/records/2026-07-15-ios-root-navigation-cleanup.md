# 2026-07-15 iOS 根页标题与资料入口收敛

## 目标

- 隐藏首页、备课、我的三个 Tab 根页面的导航栏标题，释放顶部空间。
- 资料入口保留“编辑”文字，移除重复的箭头图标。

## 边界

- 保留二级页面导航标题和返回能力。
- 不调整页面数据、业务交互或小程序实现。

## 验证

- iOS Simulator Debug 构建。
- `git diff --check`。

## 结果

- 三个根页使用 `.toolbar(.hidden, for: .navigationBar)`，二级页面继续显示各自标题和返回入口。
- 资料卡操作提示由“箭头 + 编辑”收敛为“编辑”。
- Simulator Debug 构建与 `git diff --check` 均通过。
