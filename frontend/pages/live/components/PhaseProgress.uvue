<script setup>
defineProps({
  phases: {
    type: Array,
    default: () => []
  },
  currentIndex: {
    type: Number,
    default: 0
  }
})
</script>

<template>
  <view class="phase-progress">
    <view
      v-for="(phase, index) in phases"
      :key="index"
      :class="['phase-item', { active: index === currentIndex, done: index < currentIndex }]"
    >
      <view class="phase-dot"></view>
      <text class="phase-name">{{ phase.name }}</text>
    </view>
  </view>
</template>

<style lang="scss" scoped>
.phase-progress {
  display: flex;
  align-items: center;
  padding: $spacing-md;
}
.phase-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex: 1;
  position: relative;

  &.active .phase-dot {
    background: $brand-primary;
    transform: scale(1.2);
  }

  &.done .phase-dot {
    background: $success-color;
  }
}
.phase-dot {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: $bg-tertiary;
  margin-bottom: $spacing-xs;
  transition: all 0.3s ease;
}
.phase-name {
  font-size: $font-size-xs;
  color: $text-tertiary;
}
</style>
