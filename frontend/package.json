{
  "name": "trainer-toolbox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node tests/prepare-uts.js && vitest",
    "test:run": "node tests/prepare-uts.js && vitest run",
    "test:coverage": "node tests/prepare-uts.js && vitest run --coverage",
    "test:prepare": "node tests/prepare-uts.js"
  },
  "dependencies": {
    "pinia": "^2.1.7"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "@vue/test-utils": "^2.4.5",
    "jsdom": "^24.1.0",
    "sass": "^1.77.0",
    "vitest": "^1.6.0"
  }
}
