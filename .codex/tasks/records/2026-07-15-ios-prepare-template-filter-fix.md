# 2026-07-15 iOS 备课“全部”误显示模板修复

## 目标

- 修复备课页来源为“全部”时错误混入公共模板的问题。
- 保留个人模板、公共模板筛选以及新建方案模板选择弹窗。

## 范围

- 仅修改 iOS 备课列表的模板可见性判断。
- 不修改小程序、CloudBase、模板数据或导航流程。

## 实际修改

- 将 iOS `visibleTemplates` 在来源为“全部”时的结果改为空集合，与小程序 `filterPlans` 排除模板的规则一致。
- “个人模板”继续读取用户模板，“公共模板”继续读取公共模板目录。

## 验证结果

- `swift test`：8 项通过。
- iOS Simulator Debug `xcodebuild`：通过。
- `git diff --check`：通过。
