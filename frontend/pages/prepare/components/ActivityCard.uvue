<script setup>
defineProps({
  activity: {
    type: Object,
    default: () => ({})
  }
})

defineEmits(['click', 'favorite'])
</script>

<template>
  <view class="activity-card" @click="$emit('click', activity)">
    <view class="activity-header">
      <text class="activity-name">{{ activity.name }}</text>
      <view :class="['badge', 'badge-primary']">{{ activity.categoryLabel }}</view>
    </view>
    <text class="activity-duration">{{ activity.duration }}分钟</text>
  </view>
</template>

<style lang="scss" scoped>
.activity-card {
  background: $bg-primary;
  border-radius: $radius-lg;
  padding: $spacing-md;
  margin-bottom: $spacing-md;
  box-shadow: $shadow-sm;
}
.activity-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: $spacing-xs;
}
.activity-name {
  font-size: $font-size-md;
  font-weight: 600;
  color: $text-primary;
}
.activity-duration {
  font-size: $font-size-sm;
  color: $text-tertiary;
}
</style>
