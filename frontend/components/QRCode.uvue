<script setup>
defineProps({
  text: {
    type: String,
    default: ''
  },
  size: {
    type: Number,
    default: 200
  }
})
</script>

<template>
  <view class="qrcode">
    <canvas
      :style="{ width: size + 'rpx', height: size + 'rpx' }"
      canvas-id="qrcode-canvas"
    ></canvas>
    <text v-if="text" class="qrcode-text">{{ text }}</text>
  </view>
</template>

<style lang="scss" scoped>
.qrcode {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: $spacing-md;
}
.qrcode-text {
  font-size: $font-size-sm;
  color: $text-secondary;
  margin-top: $spacing-sm;
}
</style>
